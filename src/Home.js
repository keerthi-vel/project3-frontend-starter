import React from 'react';
// import axios from 'axios';
import './App.css';

class Home extends React.Component{
    render(){
        return(
            <div id="HomeDiv">
                {/* Dont worry about this, eventually if we can i'd like to focus on the 
            main features. If we end up having time to olay around then we can work ona home page. */}
            </div>
        )
    }
}

export default Home;